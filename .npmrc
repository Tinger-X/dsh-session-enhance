# dsh-session-enhance 项目级 registry：官方源。
# 本机 ~/.npmrc 配置了 npmmirror 镜像，其 @deepseek-ai/* 预发布版本元数据
# 滞后（如 dsh-invariants 缺少 0.1.1-rc.2），导致 peer/dev 依赖无法解析。
# 与参考插件 README 推荐的 --registry=https://registry.npmjs.org/ 一致。
registry=https://registry.npmjs.org/
